import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  startButton: {
    'align-self': 'center',
    paddingTop: 30,
  },
  gameCard: {
    width: '50%',
    'align-self': 'center',
  },
});

export default useStyles;
