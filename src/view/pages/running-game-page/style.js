import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  throwButton: {
    'align-self': 'center',
  },
  containerFrameCards: {
    marginTop: '20px',
  },
});

export default useStyles;
